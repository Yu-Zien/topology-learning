# 欧几里得空间中的线段路径

固定编号：T05-02-M006  
内容版本：0.1  
教材出处：《没有眼泪的拓扑》，书内第 80 页 / PDF 第 80 页，例5.2.5。  
学习目标：对 $\mathbb R^n$ 中任意两点构造线段路径，并逐坐标验证连续性与端点条件。

> **本段主线：**把实数区间的线性插值推广到有限维欧几里得空间。  
> **当前环节：**M005已在一维中使用 $f(t)=(1-t)a+tb$。  
> **这一步只做：**让同一个公式同时作用于n个坐标。

来源说明：教材例5.2.5给出 $\mathbb R^n$ 路径连通的结论；本模块补出显式路径与验证。A、B为新增讲练。

## 例题 A

在 $\mathbb R^2$ 中，从 $a=(1,2)$ 到 $b=(3,-2)$ 写出线段路径。

## A 解答

<details><summary>展开 A</summary>

取
$$
f(t)=(1-t)a+tb=(1+2t,2-4t).
$$
两个坐标都是t的一次函数，所以f连续；并且
$$
f(0)=(1,2)=a,\qquad f(1)=(3,-2)=b.
$$

</details>

## 练习 B

设
$$
a=(a_1,\ldots,a_n),\qquad b=(b_1,\ldots,b_n)
$$
是 $\mathbb R^n$ 中任意两点。证明
$$
f(t)=(1-t)a+tb
$$
是一条连接a到b的路径，从而 $\mathbb R^n$ 路径连通。

## B 解答

<details><summary>尝试后展开完整证明</summary>

逐坐标写成
$$
f(t)=\bigl((1-t)a_1+tb_1,\ldots,(1-t)a_n+tb_n\bigr).
$$
每个坐标函数都是t的一次函数，因此连续；欧几里得空间中的坐标映射合成后，f连续。对每个 $t\in[0,1]$，上述n元组仍属于 $\mathbb R^n$。

端点为
$$
f(0)=a,\qquad f(1)=b.
$$
所以任意两点都能由线段路径连接，$\mathbb R^n$ 路径连通。

</details>

## 简短回顾

有限维欧几里得空间中的线性插值逐坐标连续，并直接连接任意两点。
