# 0.3.0 发布说明

本次只发布已认可的连续知识点交互与必要说明，没有扩大课程制作。
保留35份课件、固定编号、数学正文、MathML、折叠解答、IndexedDB及ts-fsrs 5.4.2。
源课件内容版本与网站版本分别标示。

## 学习流程

- 普通题“会做／还不会”只在原处记录表现；不滚动、不换题、不完成知识点。
- 知识点末尾“本知识点学完，继续”才推进。未评价保持未评价，没有答对或订正门槛。
- 精选FSRS和普通题再练出现在知识点结束后的同一区域，自评不替换整段，末尾统一继续。
- 教材习题入口按章、节、题号提供完整现有题设与解答，只读，不评分或改进度。

## 两个设置的实际口径（不改变0.3.0调度逻辑）

1. **每学完N个新知识点，检查精选FSRS**：只在一个普通知识点首次完成时累计1次。自评、查阅、重复完成、内容暂缓不增加此计数；升级沿用已有累计值。达到N且有可安排的已到期／首次巩固R项时才显示。没有可显示条目就保留计数，在后续知识点结束继续检查。
2. **每次精选FSRS上限N项**：实际取`min(N, 2)`，已到期项优先于首次巩固项。
3. **普通题再练不共用这个数量上限**：每次知识点结束另外检查再练，至多加1题。因此N=1时合计最多2项，N≥2时合计最多3项。普通题再练单独成块也会重置第1项的累计计数；块创建及末尾继续时都会归零。这个细节意味着它不是两套互不影响的独立计数器。

普通再练还要求留出间隔：首次评价后至少结束当前及两个后续知识点，普通题至少30分钟、题干含证明要求的题至少一天。再次出现时会预留下一间隔，跳过也不会立即重弹；后来明确会做则移出候选。普通再练不是FSRS卡。

## 备份与兼容

请在实际学习使用的本地浏览器中点“目录与进度 → 导出备份”，保存当前记录。
这份个人文件由使用者自行保存，不在此公开仓库中；测试记录不能替代它。

线上地址和本机地址的浏览器存储独立。如需延续本机进度，先从原地址导出，再到线上恢复。
数据库仍为`topology-learning-v1`，对象仓库仍为`learning`；不清库、不迁移存储位置。
旧“已练习”仅保留为做过证据，不变成会做，也不生成FSRS成功评分。

**升级前备份**是在该浏览器第一次将已有、尚无study字段的旧状态写成连续学习格式时，同事务保留的旧状态。仅保留一次，已有快照不覆盖。新建空库、仅查资料、单纯读取不会生成；不是每次启动或每次导入的当前进度自动备份。如果浏览器已是0.3.0，会继续保留原快照，不再生成新的“升级前”副本。请用“导出备份”保存当前进度。

## 撤销评分

撤销全部题目中最近一条尚未撤销的自评，范围包括普通题、再练、FSRS，不限于当前题。
记录会留下撤销标记；当前回顾中的相应评价会解除。它不撤销知识点完成、不倒退主线，也不删除旧“已练习”证据。

- FSRS：恢复该次评分前的卡片；若之前没有卡片则移除新卡。
- 普通题／再练：按上一条有效自评重新计算候选；若上一条为还不会，间隔从撤销时重新留出，并非恢复旧的到期时间。
- 已经结束的回顾块不会因撤销自动重新打开。

## 回退与核验

旧网站代码保留为`v0.2.0`标签（提交`a384789eb224191297e396e973bf9579c551d856`），用于代码回退；本版会标记`v0.3.0`。
代码回退与个人数据回退是两回事：保留当前完整导出和升级前备份，回退前需核对数据兼容，不通过删库或清进度解决。含新版retry记录的备份没有保证可被旧0.2.0读取。

本次只上传网页、运行时、公开课件及脱敏说明，不上传个人备份、学习进度、教材PDF、日志或凭据。
以GitHub Release中的发布提交、文件清单和实际线上验收摘要为准，不把推送成功当作部署完成。
未测试的浏览器及网络范围会在验收摘要中明确列出。
